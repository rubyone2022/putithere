# Source Directory

This placeholder keeps the proposed `src/` directory visible in Git.

The repository currently contains workflow documentation rather than an application implementation. When source code is added:

- document the architecture in `docs/PROJECT.md`;
- record the real state in `docs/STATE.md`;
- add commands and constraints to `AGENTS.md`;
- keep tests and generated artifacts outside this placeholder document as appropriate.

Delete or replace this file once the directory contains actual source files.
